# 个人品牌网站 — 第一阶段方案（欢迎页 · 纯静态）

> 网名：曜
> 内容方向：公司财务整体 AI 化 + 生活旅游类
> 一期目标：个人 IP 展示 + 多平台引流（纯静态欢迎页）
> 域名：iyao.com
> 文档版本：v1.1（最终确认版）
> 更新日期：2026-09-17

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术方案](#2-技术方案)
3. [欢迎页设计](#3-欢迎页设计)
4. [工程结构](#4-工程结构)
5. [开发路线图](#5-开发路线图)
6. [部署方案](#6-部署方案)
7. [待确认事项](#7-待确认事项)

---

## 1. 项目概述

### 1.1 一期范围

第一阶段仅包含**欢迎页**，纯静态站点（Next.js `output: 'export'`），无需后端、无需数据库。

```
┌─────────────────────────────────────────────┐
│              一期：欢迎页（纯静态）            │
│                                             │
│  · 流动光晕背景（Canvas）                     │
│  · 首屏标题渐入动画                           │
│  · 打字机自我介绍                             │
│  · 多平台引流图标（微信视频号/抖音/B站/小红书） │
│  · 微信公众号二维码弹窗                        │
│  · 响应式适配（Desktop / Tablet / Mobile）     │
│  · SEO 基础优化                               │
│                                             │
│  部署：Nginx → 静态文件                        │
│  无需：MySQL、Redis、Node 运行时、PM2          │
└─────────────────────────────────────────────┘
```

### 1.2 分期规划（全貌）

| 阶段 | 内容 | 本次范围 |
|------|------|----------|
| **一期** | 欢迎页（纯静态） | ✅ 本次 |
| **二期** | 往期视频归档 | 后续 |
| **三期** | 后台管理系统 | 后续 |

---

## 2. 技术方案

| 类型 | 技术 | 说明 |
|------|------|------|
| 前端框架 | Next.js 15 (App Router) | `output: 'export'` 纯静态 |
| 语言 | TypeScript 5 | 类型安全 |
| 样式 | Tailwind CSS 4 | CSS-first（`@theme` 定义色板） |
| 动画 | Framer Motion 12 | React 动画 |
| 包管理 | **npm workspaces** | Monorepo（本机未装 pnpm，改用 npm） |
| 构建 | npm scripts | 根脚本驱动（`turbo.json` 保留但未启用） |
| 代码规范 | ESLint + Prettier | 代码质量 |

> 一期先只建 `apps/web`，后端 `apps/server` 和共享包 `packages/shared` 留到二期再搭，避免过度工程化。
>
> ⚠️ 与原 v1.0 方案的差异：包管理由 pnpm 改为 **npm workspaces**（适配本机 Windows，未安装 pnpm）；Tailwind 采用 **4.x CSS-first 配置**（`postcss.config.mjs` + `globals.css` 的 `@theme`），不再使用 `tailwind.config.ts`。

---

## 3. 欢迎页设计

### 3.1 视觉系统

#### 配色方案

```
暗黑底色
  bg-primary:    #08080c  (最深黑)
  bg-secondary:  #111118  (卡片底)
  bg-tertiary:   #1a1a24  (悬浮态)

生活旅游 — 暖金系
  accent-warm:        #d4a574  (主强调)
  accent-warm-glow:   rgba(212, 165, 116, 0.3)

财务 AI — 冷色系
  accent-cool:        #5eead4  (主强调)
  accent-cool-glow:   rgba(94, 234, 212, 0.3)

中性色
  text-primary:       #e4e4e7  (正文)
  text-secondary:     #a1a1aa  (辅助文字)
  border:             rgba(255,255,255,0.06)
  border-hover:       rgba(255,255,255,0.12)
```

#### 设计原则

- **少即是多**：每屏信息量克制，大量留白
- **光影感**：微妙的渐变光晕 + 毛玻璃卡片 + 细边框
- **对比克制**：不刺眼，所有颜色在暗黑底上收一档饱和度
- **温暖与冷静并存**：暖金和冷色在页面不同区域交替出现，映射两个内容方向

### 3.2 文案设计

#### 自我介绍（打字机效果逐字呈现）

```
第一段（打招呼）：
"Hey，我是曜 👋"

第二段（身份定位）：
"一名深耕财务、拥抱 AI 的实践者。"

第三段（内容方向）：
"在这里，我会分享财务 AI 化的实战经验，"
"也会带你去看山川湖海的风景。"

第四段（价值承诺）：
"让工作更高效、让生活更有趣，"
"我们一起。"
```

> 共计约 70 字，打字机效果控制在 5-8 秒播完。

#### 各平台标签文案

| 平台 | 标签文案 |
|------|----------|
| 微信视频号 | 全部内容 |
| 抖音 | 全部内容 |
| 哔哩哔哩 | 财务 AI |
| 小红书 | 生活旅游 |
| 微信公众号 | 扫码关注 |

### 3.3 Mock 数据方案

在拿到真实的平台链接和二维码之前，先用占位数据：

```typescript
// src/lib/platforms.ts

const platforms = [
  {
    id: 'wechat-video',
    name: '微信视频号',
    label: '全部内容',
    url: 'https://channels.weixin.qq.com/user/placeholder',  // ← 待替换
    icon: 'video',
    color: 'accent-cool',
  },
  {
    id: 'douyin',
    name: '抖音',
    label: '全部内容',
    url: 'https://www.douyin.com/user/placeholder',  // ← 待替换
    icon: 'music',
    color: 'accent-cool',
  },
  {
    id: 'bilibili',
    name: '哔哩哔哩',
    label: '财务 AI',
    url: 'https://space.bilibili.com/placeholder',  // ← 待替换
    icon: 'tv',
    color: 'accent-cool',
  },
  {
    id: 'xiaohongshu',
    name: '小红书',
    label: '生活旅游',
    url: 'https://www.xiaohongshu.com/user/placeholder',  // ← 待替换
    icon: 'book',
    color: 'accent-warm',
  },
]

const wechatPublic = {
  name: '微信公众号',
  label: '扫码关注',
  qrcodePath: '/images/qrcode-wechat.svg',  // ← 待替换为真实二维码
  color: 'accent-warm',
}
```

> 占位图为 SVG（`logo.svg` / `qrcode-wechat.svg` / `og-image.svg`），拿到真实素材后直接替换同名文件即可。

### 3.4 页面布局 & 交互

```
┌────────────────────────────────────────────────────────┐
│                                                        │
│              ╔══════════════════════╗                   │
│              ║   流动光晕背景        ║  ← 全屏 Canvas  │
│              ║   暖金+冷色光晕缓慢流动║    动态光晕     │
│              ╚══════════════════════╝                   │
│                                                        │
│  ┌──────────────────────────────────────────────────┐  │
│  │              ┌──────────┐                        │  │
│  │              │  🧑‍💻 Logo │  ← 圆形头像/Logo       │  │
│  │              └──────────┘     (Mock 占位)        │  │
│  │               曜                                  │  │
│  │           ─────── ✦ ───────                      │  │
│  │        财务 AI 化  ×  生活旅游                     │  │
│  │       (视口居中，垂直方向约 40vh 位置)             │  │
│  └──────────────────────────────────────────────────┘  │
│                   ↓ 向下滚动触发                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Hey，我是曜 👋                                   │  │
│  │  一名深耕财务、拥抱 AI 的实践者。                   │  │
│  │  在这里，我会分享财务 AI 化的实战经验，             │  │
│  │  也会带你去看山川湖海的风景。                       │  │
│  │  让工作更高效、让生活更有趣，                       │  │
│  │  我们一起。                                       │  │
│  │                                     ▎(光标闪烁)  │  │
│  └──────────────────────────────────────────────────┘  │
│                   ↓ 继续向下滚动                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │              🔗  找到我                           │  │
│  │   ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐        │  │
│  │   │ 微信 │  │ 抖音 │  │ B站  │  │小红书│        │  │
│  │   │视频号│  │      │  │      │  │      │        │  │
│  │   │全内容│  │全内容│  │财务AI│  │旅游  │        │  │
│  │   └──────┘  └──────┘  └──────┘  └──────┘        │  │
│  │        ┌──────────────────────┐                  │  │
│  │        │    📱 微信公众号      │                  │  │
│  │        │    [点击查看二维码]   │  ← 胶囊按钮      │  │
│  │        └──────────────────────┘                  │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │         © 2026 曜                                │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
```

#### 交互细节表

| 元素 | 触发条件 | 动画效果 | 时长 |
|------|----------|----------|------|
| 标题 | 页面加载 | fade-in + translateY(-20px→0) | 0.8s |
| 副标题 | 标题完成后 | fade-in + 金色到蓝色渐变流动 | 0.6s |
| 自我介绍 | 滚动到该区域 (IntersectionObserver) | 打字机逐字出现 + 光标闪烁 | ~5-8s |
| 平台图标 | 进入视口，依次弹入 | stagger each 0.1s + spring 弹性 | 0.5s/个 |
| 平台图标 | hover | scale(1.08) + 外发光 8px + 边框变亮 | 0.2s |
| 平台图标 | click | 新标签页打开平台主页 | — |
| 微信按钮 | click | 从图标位置展开模态框 | 0.3s |
| 模态框遮罩 | 出现 | backdrop-filter: blur(8px) + 暗色渐变 | 0.3s |
| 模态框内容 | 遮罩完成后 | scale(0.9→1) + fade-in | 0.25s |
| 模态框关闭 | 点遮罩 / 按 ESC / 点关闭按钮 | 反向动画 | 0.2s |

### 3.5 响应式断点设计

| 断点 | 宽度 | 设计行为 |
|------|------|----------|
| **Desktop** | ≥1280px | 标准布局，平台图标 5 个一行，最大宽度 1200px 居中 |
| **Laptop** | 1024-1279px | 容器宽度 90vw，字号缩小 5%，图标间距收紧 |
| **Tablet** | 768-1023px | 自我介绍字号缩小，平台图标 3+2 两行排列 |
| **Mobile** | <768px | 单列垂直布局，标题字号 2xl，自我介绍字号 base，平台图标每行 2 个，模态框全屏，粒子密度降低 |

#### 移动端专项优化

- 粒子 Canvas 在 `<768px` 时粒子数量减半
- 平台图标最小触摸区域 44×44px（WCAG 无障碍标准）
- 模态框在移动端改为底部抽屉式（Bottom Sheet）
- 滚动视差效果在移动端禁用
- 字体大小使用 `clamp()` 做流式缩放

### 3.6 组件设计规格

| 组件 | 职责 | Props | 状态 |
|------|------|-------|------|
| `GlowBackground` | 全屏 Canvas 流动光晕系统 | `colorWarm`, `colorCool` | 纯渲染，响应鼠标靠近 |
| `HeroSection` | 首屏容器，居中布局 | `children` | 无 |
| `AnimatedTitle` | 标题渐入 + 副标题流光 | — | `entering` → `visible` |
| `IntroSection` | 自我介绍区域容器 | `children` | 监听 IntersectionObserver |
| `TypewriterText` | 打字机效果 | `text: string[]`, `speed?: number` | `idle` → `typing` → `done` |
| `PlatformSection` | 平台引流区域 | — | 无 |
| `PlatformIcon` | 单个平台圆形图标 | `platform: Platform`, `onClick` | `idle`, `hover`, `active` |
| `WechatQRButton` | 微信按钮（特殊样式） | `onClick` | `idle`, `hover` |
| `QRCodeModal` | 二维码模态框 | `isOpen`, `onClose`, `qrcodeSrc` | `closed` → `opening` → `open` → `closing` |
| `Footer` | 页脚 | — | 静态 |

---

## 4. 工程结构

### 4.1 目录结构

```
yao-site/
│
├── package.json                 # 根 package.json（npm workspaces）
├── turbo.json                   # Turborepo 配置（保留，当前未启用）
├── tsconfig.base.json
├── .eslintrc.js
├── .prettierrc
├── .gitignore
├── README.md
│
├── apps/
│   └── web/                     # Next.js 前端（一期只建这个）
│       ├── package.json
│       ├── next.config.ts       # output: 'export' + images.unoptimized
│       ├── postcss.config.mjs   # @tailwindcss/postcss（Tailwind 4 CSS-first）
│       ├── tsconfig.json        # 路径别名 @/* → ./src/*
│       ├── next-env.d.ts
│       ├── public/
│       │   ├── images/
│       │   │   ├── logo.svg             # 头像/Logo（SVG 占位）
│       │   │   ├── qrcode-wechat.svg    # 占位二维码
│       │   │   └── og-image.svg
│       │   └── robots.txt
│       └── src/
│           ├── app/
│           │   ├── layout.tsx           # metadata + OG 标签
│           │   ├── page.tsx             # 欢迎页
│           │   ├── globals.css          # @theme 定义色板
│           │   └── icon.svg             # 站点图标（favicon）
│           ├── components/
│           │   └── welcome/
│           │       ├── GlowBackground.tsx
│           │       ├── HeroSection.tsx
│           │       ├── AnimatedTitle.tsx
│           │       ├── IntroSection.tsx
│           │       ├── TypewriterText.tsx
│           │       ├── PlatformSection.tsx
│           │       ├── PlatformIcon.tsx
│           │       ├── WechatQRButton.tsx
│           │       ├── QRCodeModal.tsx
│           │       └── Footer.tsx
│           ├── hooks/
│           │       ├── useTypewriter.ts
│           │       ├── useScrollTrigger.ts
│           │       └── useMediaQuery.ts
│           ├── lib/
│           │       ├── platforms.ts      # 平台数据配置
│           │       └── constants.ts      # 全局常量（站点信息/文案）
│           └── types/
│               └── index.ts
│
└── scripts/
    ├── deploy.ps1                 # 部署脚本（Windows PowerShell）
    └── deploy.sh                  # 部署脚本（Linux）
```

### 4.2 核心依赖

```json
// apps/web/package.json (关键依赖)
{
  "dependencies": {
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "framer-motion": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "*",
    "@types/react": "*",
    "@types/react-dom": "*",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/postcss": "^4.0.0"
  }
}
```

> Tailwind 4 采用 CSS-first 配置，色板在 `globals.css` 的 `@theme` 块中定义，**无** `tailwind.config.ts`。

---

## 5. 开发路线图

| 步骤 | 任务 | 说明 | 状态 |
|------|------|------|------|
| 1 | 初始化工程 | npm workspace + 根配置（一期只建 `apps/web`） | ✅ |
| 2 | 搭建 `apps/web` | Next.js 15 + Tailwind 4 + TypeScript | ✅ |
| 3 | 暗黑主题系统 | CSS 变量（`@theme`）+ 自定义色板 | ✅ |
| 4 | GlowBackground | Canvas 流动光晕背景 | ✅ |
| 5 | HeroSection + AnimatedTitle | 首屏标题渐入动画 | ✅ |
| 6 | IntroSection + TypewriterText | 打字机自我介绍 | ✅ |
| 7 | PlatformSection + PlatformIcon | 平台图标组件 | ✅ |
| 8 | WechatQRButton + QRCodeModal | 微信二维码弹窗 | ✅ |
| 9 | Footer | 页脚 | ✅ |
| 10 | 响应式适配 | Mobile / Tablet / Desktop 三断点 | ✅ |
| 11 | SEO | metadata、OG 标签、robots.txt（sitemap 待补） | 🔶 |
| 12 | 构建 & 部署 | `output: 'export'` + Nginx 部署验证 | ⬜ 待上线 |

---

## 6. 部署方案

一期只有静态文件，部署极简：

```powershell
# Windows（本机，PowerShell）
cd C:\Users\A\yao-site
npm install
npm run build                 # → 产物在 apps/web/out/
.\scripts\deploy.ps1 -Server "user@server" -RemotePath "/opt/site/"
```

```bash
# Linux
cd yao-site
npm install
npm run build --workspace @yao/web
scp -r apps/web/out/* user@server:/opt/site/
```

Nginx 极简配置：

```nginx
server {
    listen 80;
    server_name iyao.com;

    location / {
        root /opt/site;
        index index.html;
        try_files $uri $uri.html $uri/ =404;
    }

    # 静态资源长期缓存
    location /_next/static/ {
        root /opt/site;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 图片缓存
    location /images/ {
        root /opt/site;
        expires 7d;
        add_header Cache-Control "public";
    }
}
```

> 无需 MySQL、Redis、Node 运行时、PM2。Nginx 直接托管静态文件即可。

---

## 7. 待确认事项

### 已确定 ✅

| 项 | 决策 |
|----|------|
| 域名 | **iyao.com** |
| 前端框架 | Next.js 15 |
| 样式方案 | Tailwind CSS 4（CSS-first） |
| 动画库 | Framer Motion 12 |
| 包管理 | npm workspaces（本机未装 pnpm，改用 npm） |
| 配色方案 | 暖金 `#d4a574` + 冷青 `#5eead4` 双色 |
| 背景风格 | 流动光晕（暖金+冷色在暗黑底上缓慢流动，鼠标靠近有响应） |
| Logo | 先用 SVG 占位图，后续替换 |
| 自我介绍文案 | 已确认，见 3.2 节 |
| 平台链接 | 先 Mock 占位，后续替换真实链接 |
| 二维码 | 先 SVG 占位图，后续替换真实公众号二维码 |
| 部署方式 | 自部署 Nginx |
| 响应式 | Desktop / Tablet / Mobile 三断点 |
| 一期范围 | 仅欢迎页，纯静态（`output: 'export'`） |

### 仍需确认 🔴

| # | 问题 |
|---|------|
| 1 | **Logo 风格**：偏好什么风格？简约文字 / 抽象图形 / 卡通头像？有没有参考？ |
| 2 | **流动光晕细节**：背景光晕是否有参考效果图？比如 Apple 官网那种微妙的渐层光晕，还是更明显的流体动效？ |

---

> 📝 本文档为第一阶段（欢迎页·纯静态）最终确认版，已与代码实现对齐（npm workspaces + Tailwind 4 CSS-first + Windows 部署）。可直接用于开发实施与维护。
